# Optional Company Pack umbrella Host row.
# Desktop always inserts dsh-plugin-company-example separately for the Settings hub.
# This pack stays opt-in and is never default-inserted by desktop cordis.patch.yml.
- insert:
    - id: company-pack
      name: dsh-plugin-company-pack
